spring.application.name=sync-study-backend
spring.datasource.url=jdbc:mysql://localhost:3306/sync_study?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=565627
spring.sql.init.mode=always
spring.sql.init.encoding=UTF-8
server.address=0.0.0.0
app.jwt.secret=${JWT_SECRET:c3luYy1zdHVkeS1kZXZlbG9wbWVudC1zZWNyZXQta2V5LTIwMjY=}
app.jwt.expiration-seconds=604800
app.agent.base-url=${AGENT_SERVICE_BASE_URL:http://127.0.0.1:8001}
app.agent.service-token=${AGENT_SERVICE_TOKEN:}
app.agent.connect-timeout=${AGENT_CONNECT_TIMEOUT:3s}
app.agent.read-timeout=${AGENT_READ_TIMEOUT:45s}
# 同一令牌同时用于 Java -> Agent 与 Agent -> Java 的服务间鉴权；生产环境必须通过环境变量提供。
app.agent.internal-api-token=${AGENT_SERVICE_TOKEN:}
